<IfModule mod_rewrite.c>
    # On shared hosting the domain document root is often public_html.
    # Route all requests into Laravel's public/ folder.
    RewriteEngine On
    RewriteRule ^(.*)$ ../public/$1 [L]
</IfModule>
